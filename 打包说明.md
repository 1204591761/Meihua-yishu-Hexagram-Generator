# 梅花易数 安卓 App 打包说明

## 项目结构

```
梅花易数Android/
├── app/
│   ├── build.gradle                     # App 构建配置
│   ├── proguard-rules.pro
│   └── src/main/
│       ├── AndroidManifest.xml          # 权限声明
│       ├── assets/
│       │   └── index.html              ← H5 主体（含完整业务逻辑）
│       ├── java/com/meihua/yishu/
│       │   └── MainActivity.java        ← WebView 壳 + 剪贴板桥接
│       └── res/
│           ├── mipmap-*/               ← App 图标（4个尺寸）
│           └── values/                 ← 字符串、主题
├── build.gradle                         # 项目构建配置
├── settings.gradle
└── gradle/wrapper/
    └── gradle-wrapper.properties
```

---

## 打包步骤（使用 Android Studio）

### 第一步：安装 Android Studio

下载地址：https://developer.android.com/studio

安装时勾选 **Android SDK（API 34）**，等待安装完成。

---

### 第二步：导入项目

1. 打开 Android Studio
2. 选择 **Open**（不是 New Project）
3. 选择 `D:\desktop\梅花易数Android` 文件夹
4. 等待 Gradle 自动同步（首次需要下载依赖，需要联网，约3-10分钟）

---

### 第三步：运行测试（可选）

- 连接安卓手机，开启「USB 调试」
- 点击工具栏绿色 ▶ 运行按钮
- 或使用模拟器（AVD Manager）

---

### 第四步：打包 APK

**方式一：Debug APK（直接安装，无需签名）**

```
菜单栏 → Build → Build Bundle(s)/APK(s) → Build APK(s)
```

等待完成后，点击右下角提示的 **locate** 按钮，找到 APK 文件：

```
app/build/outputs/apk/debug/app-debug.apk
```

**方式二：Release APK（正式发布版，需要签名）**

```
菜单栏 → Build → Generate Signed Bundle/APK
→ 选择 APK
→ 创建或选择 KeyStore（签名证书）
→ 填写密码、别名
→ 选择 release 构建类型
→ Finish
```

签名后的 APK 在：

```
app/build/outputs/apk/release/app-release.apk
```

---

## 安装 APK 到手机

### 方法一：USB 传输

1. 手机连接电脑
2. 将 APK 文件复制到手机存储
3. 手机端点击 APK 文件安装（需开启「允许未知来源」）

### 方法二：微信/QQ 传输

直接把 APK 文件发到自己的微信文件传输助手，在手机端打开安装。

---

## App 功能说明

| 功能 | 说明 |
|------|------|
| 起卦 | 九位数输入，实时拆分预览，计算本/互/变卦 |
| 卦象图 | SVG 绘制，动爻红色闪烁动画 |
| AI 解卦 | 支持 DeepSeek / OpenAI 接口（需自备 API Key） |
| 复制功能 | 调用 Android 原生剪贴板，支持提问/验证/卦象三种格式 |
| 历史记录 | localStorage 持久化，最多保存 20 条 |
| 模板编辑 | 提问模板和验证文案均可自定义 |

---

## 常见问题

**Q：首次打开网络资源（Tailwind/FontAwesome）加载慢？**  
A：App 需要联网加载 CDN 资源（Tailwind CSS / FontAwesome 字体），首次打开建议在有网络的环境下使用。也可以将这两个资源下载后放入 assets 目录，修改 HTML 中的引用路径即可完全离线运行。

**Q：AI 解卦不可用？**  
A：需要在 App 内「AI 解卦」区域输入 API Key。推荐使用 DeepSeek（https://platform.deepseek.com）注册后获取免费额度。

**Q：安装时提示「未知来源」？**  
A：进入手机设置 → 安全/隐私 → 允许安装未知来源应用。不同手机路径略有差异。

**Q：想上架应用商店？**  
A：需要 Release 签名包，以及在各平台注册开发者账号（国内平台需要软著）。

---

## 离线资源（可选优化）

如果想让 App 完全离线运行，将以下文件下载后放入 assets 目录：

1. Tailwind CSS：https://cdn.tailwindcss.com → 保存为 `assets/tailwind.min.js`
2. FontAwesome：下载完整包 → 放入 `assets/fonts/`

然后修改 `index.html` 中的引用：

```html
<!-- 改为本地路径 -->
<script src="tailwind.min.js"></script>
<link href="fonts/font-awesome.min.css" rel="stylesheet">
```
